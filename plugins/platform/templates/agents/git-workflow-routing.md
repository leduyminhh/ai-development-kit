### Platform Git Workflow Routing

- For Git-related requests such as commit, push, branch, merge, revert, PR,
  release, hotfix, changelog, or release notes, use
  `platform.git_workflow_design` before taking Git action.
