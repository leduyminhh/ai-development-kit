{
  "schemaVersion": 1,
  "mode": "external-runtime-only",
  "defaults": {
    "activateByDefault": false,
    "installProviderConfig": false,
    "ownProviderConfig": false
  },
  "rules": [
    "Plugins may reference skills, commands, agents, templates, workflows, and schemas.",
    "Plugins must not declare active MCP runtime servers.",
    "MCP examples are documentation only until explicitly promoted into the active registry.",
    "Provider config generated by the CLI must not create MCP registrations when activeTools is empty."
  ]
}
