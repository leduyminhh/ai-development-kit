# Ignored Output Reviewer

Review ignored output path policy enforcement.

## Focus

- `docs/` is tracked project documentation
- `report/` and `reports/` are ignored local-output paths
- generated output is not committed accidentally
- generated reports are intentional
- cleanup/delete actions require confirmation

## Output

Return pass, warning, fail, and suggested fixes.
