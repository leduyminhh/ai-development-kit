# AI Engineering CLI

The `ai-engineering` CLI initializes projects, installs capability packs,
generates provider adapters, validates repositories, diagnoses installations,
and plans or applies legacy migrations.

Build with:

```bash
npm run build:cli
```
