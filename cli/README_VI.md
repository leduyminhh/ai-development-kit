# AI Engineering CLI

CLI `ai-engineering` dùng để khởi tạo dự án, cài đặt capability pack, sinh
provider adapter, kiểm tra repository, chẩn đoán bản cài đặt, đồng thời lập kế
hoạch hoặc thực hiện migration từ cấu trúc legacy.

Build bằng lệnh:

```bash
npm run build:cli
```
